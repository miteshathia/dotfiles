# ========================================
# GENERAL SETTINGS
# ========================================

# Load bashrc and profile if they exist
if [ -f $HOME/.bashrc ]; then
    . $HOME/.bashrc
fi

if [ -f $HOME/.profile ]; then
    . $HOME/.profile
fi

